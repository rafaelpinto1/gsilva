function acessar(tipo) {
  if (tipo === "funcionario") {
    window.location.href = "funcionario.html"; // substitua pelo caminho real
  } else if (tipo === "cliente") {
    window.location.href = "cliente.html"; // substitua pelo caminho real
  }
}
